"use client";

// EdgeRenderer — custom React Flow edge for dependency visualization.
// Edge style varies by DependencyType: blocks (solid red), related (dashed),
// subtask (solid subtle).
// TODO(MVP): implement as a React Flow `EdgeProps`-consuming component.
export function EdgeRenderer() {
  return null;
}
